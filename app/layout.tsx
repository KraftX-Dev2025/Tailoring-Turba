import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/navbar"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Tailoring Turba | Custom T-Shirt Printing",
  description:
    "Design your own custom t-shirts with our easy-to-use online designer. Upload your designs, choose colors, and create your perfect t-shirt.",
  keywords: "custom t-shirts, t-shirt printing, custom apparel, design your own shirt, personalized t-shirts",
  generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
